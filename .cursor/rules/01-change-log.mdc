---
alwaysApply: true
---
## 📄 Формат записи в дневнике

Все времена в этом документе и в дневниковых записях указываются в часовом поясе Europe/Kyiv.

```markdown
## doc/workflow.md
**Entry ID:** <ULID|timestamp>
**Дата:** <YYYY-MM-DD>
**Ветка:** <branch-name>

### Файлы
- `path/to/file.php` (+10 −3)

### Что сделано
<3–4 предложения>

### Почему
<1–3 предложения>

### Влияние
- **БД:** <описание или N/A>
- **API:** <описание или N/A>
- **Производительность:** <описание или N/A>

### Проверено
- Тесты: <новые|обновлены|N/A>
- Линтер: <ok|todo>

### Follow-up
- [ ] <задачи или N/A>
```

### 🏷️ Типы изменений
- `feat` — новая функциональность
- `fix` — исправление бага
- `refactor` — рефакторинг
- `db` — изменения БД
- `perf` — производительность
- `docs` — документация
- `chore` — рутинные задачи
- `merge` — слияние веток

### ✅ Когда документировать
Документируй всегда, если изменён хотя бы один из следующих артефактов:
- Любой `.php` в `app/*` (модели, контроллеры, сервисы, миддлвары, запросы, джобы, слушатели, провайдеры, компоненты)
- Любой файл в `routes/*.php`
- Любая миграция `database/migrations/*`
- Любые API-контракты (структура ответов/запросов, ресурсы)
- Любая бизнес-логика

Не документируй только:
- Чистое форматирование (Pint), комментарии без кода, тесты без изменений кода

---

## 🔒 Строгость правила (чтобы ВСЕГДА выполнялось)

Обязательные проверки на каждом цикле работы:
1) Gate — если изменяется что-либо в:
   - `app/` (кроме чистого форматирования)
   - `routes/`
   - `database/migrations/`
   - `resources/views/`
   - `config/`
   → Запускается обязательный workflow (Шаг 1 и Шаг 2).
2) Append-only — все новые записи в `docs/changes/diary/*` и `docs/changes/tags/*` добавляются ТОЛЬКО в конец файлов; порядок старых записей сохраняется.
3) Полнота скоупов — если затронуты роуты + контроллер + модель/сервис, обнови ВСЕ соответствующие теги (а не один).
4) Отсутствующие файлы — если `docs/changes/diary/*` или `docs/changes/tags/<scope>.md` отсутствуют, создай их с корректными заголовками и структурой.
5) Статус-обновления — до запуска чтений/изменений и после завершения публикации записей всегда публикуй короткий статус (что сделал/что делаю/риски).
6) Check-list перед завершением задачи — убедись, что:
   - [ ] Дневник обновлён
   - [ ] ВСЕ релевантные теги обновлены
   - [ ] Сообщение «✅ Документация обновлена: …» отправлено

Если любой пункт невыполним (блокер) — немедленно запроси уточнение и остановись.

---

## 🧪 Примеры (адаптированы под проект)

### Пример 1: «Добавь поле tracking_code в модель Order»

Перед началом (Уровень 2 — теги):
- `model.order`
- `db.orders.migration`

Затронутые файлы:
- `app/Models/Order.php`
- `database/migrations/2025_10_09_120000_add_tracking_code_to_orders_table.php`

Скоупы: `model.order`, `db.orders.migration`

Короткая запись в дневнике (фрагмент):
```markdown
## 12:05 (Europe/Riga) feat[model.order,db.orders.migration] — tracking_code для Order
**Entry ID:** 01JAXXXXXXXTRACK
**Дата:** 2025-10-09
**Ветка:** feature/order-tracking

### Файлы
- `app/Models/Order.php` (+12 −0)
- `database/migrations/2025_10_09_120000_add_tracking_code_to_orders_table.php` (+28 −0)

### Что сделано
Добавлено поле tracking_code в БД и модель Order. Обновлены fillable и касты, добавлен индекс.

### Почему
Для отслеживания отгрузок через партнёрские сервисы.

### Влияние
- **БД:** новая колонка `tracking_code` в `orders`
- **API:** N/A
- **Производительность:** минимально

### Проверено
- Тесты: новые
- Линтер: ok
```

### Пример 2: «Создай Admin API endpoint для тарифов доставки»

Перед началом (Уровень 2 — теги):
- `api.admin.shipping-tariffs`
- `controller.admin.api.shipping.shipping-tariff`
- `service.shipping.tariff` (если есть логика)
- `model.shipping-tariff` (если используется модель)

Затронутые файлы:
- `routes/admin-api.php`
- `app/Http/Controllers/Admin/Api/Shipping/ShippingTariffController.php`
- `app/Services/Shipping/TariffService.php` (при наличии/создать)

Скоупы: `api.admin.shipping-tariffs`, `controller.admin.api.shipping.shipping-tariff`, `service.shipping.tariff`

Короткая запись в дневнике (фрагмент):
```markdown
## 14:20 (Europe/Riga) feat[api.admin.shipping-tariffs,controller.admin.api.shipping.shipping-tariff] — Admin тарифы доставки
**Entry ID:** 01JASHIPTARIFFS
**Дата:** 2025-10-09
**Ветка:** feature/admin-shipping-tariffs

### Файлы
- `routes/admin-api.php` (+8 −0)
- `app/Http/Controllers/Admin/Api/Shipping/ShippingTariffController.php` (+85 −0)

### Что сделано
Добавлен Admin API endpoint для CRUD тарифов доставки. Валидация вынесена в Form Request, доступ ограничен ролями.

### Почему
Нужно управлять тарифами без деплоя.

### Влияние
- **БД:** N/A
- **API:** новый CRUD `/admin-api/shipping/tariffs`
- **Производительность:** N/A

### Проверено
- Тесты: новые
- Линтер: ok
```

### Пример 3: «Создай Shop API endpoint для списка заказов пользователя»

Перед началом (Уровень 2 — теги):
- `api.shop.orders`
- `controller.shop.api.order`
- `model.order`

Затронутые файлы:
- `routes/api.php`
- `app/Http/Controllers/Shop/Api/Order/OrderController.php`

Скоупы: `api.shop.orders`, `controller.shop.api.order`, `model.order`

Короткая запись (фрагмент):
```markdown
## 15:10 (Europe/Riga) feat[api.shop.orders,controller.shop.api.order] — Список заказов пользователя
**Entry ID:** 01JAORDERSLIST
**Дата:** 2025-10-09
**Ветка:** feature/shop-orders-index

### Файлы
- `routes/api.php` (+6 −0)
- `app/Http/Controllers/Shop/Api/Order/OrderController.php` (+60 −0)

### Что сделано
Добавлен endpoint GET `/api/orders` для авторизованного пользователя с пагинацией и сортировкой. Eager loading для избежания N+1.

### Почему
Необходимо для личного кабинета.

### Влияние
- **БД:** N/A
- **API:** новый endpoint (обратно совместим)
- **Производительность:** оптимизировано загрузкой связей

### Проверено
- Тесты: новые
- Линтер: ok
```

### Пример 4: «Добавить валидацию в админ-логин»

Перед началом (Уровень 2 — теги):
- `request.admin.auth.login`
- `controller.admin.web.auth`

Затронутые файлы:
- `app/Http/Requests/Admin/Auth/LoginRequest.php`
- `app/Http/Controllers/Admin/Web/Auth/LoginController.php`

Скоупы: `request.admin.auth.login`, `controller.admin.web.auth`

---

## 🔀 Специальный случай: MERGE

Формат записи:
```markdown
## <HH:MM (Europe/Riga)> merge[<главные-скоупы>] — Merge <source-branch> → <target-branch>
**Entry ID:** <ULID|timestamp>
**Дата:** <YYYY-MM-DD>
**Merge:** <source-branch> → <target-branch>

### Ветки
- **Source:** <source-branch>
- **Target:** <target-branch>
- **Merge commit:** <hash> (если есть)

### Файлы
- `path/to/file1.php` (+15 −3)
- `path/to/file2.php` (+42 −0)
… (+N файлов)

### Что смерджено
<3–4 предложения>

### Конфликты
- **Были конфликты:** <да|нет>
- **Файлы с конфликтами:** <список или N/A>
- **Как разрешены:** <кратко или N/A>

### Влияние
- **БД:** <миграции или N/A>
- **API:** <breaking changes или N/A>
- **Производительность:** <N/A или описание>

### Проверено
- Тесты: <запущены|N/A>
- Линтер: <ok|todo>
- Конфликты: <разрешены|N/A>

### Follow-up
- [ ] <задачи или N/A>
```

Алгоритм:
1) Получи: source, target, были ли конфликты, (опц.) merge hash
2) Составь список файлов из merge (diff/stat)
3) Определи главные скоупы (1–3)
4) Создай запись как выше
5) Обнови ВСЕ теги для затронутых скоупов
6) Сообщи: «✅ Merge задокументирован: <source> → <target>, скоупы: <…>»

---

## 📝 Формат обновления тегов

Каждый `docs/changes/tags/<scope>.md`:

```markdown
# Изменения: <scope>

- <YYYY-MM-DD> — <описание> → [../../diary/YYYY/MM/YYYY-MM-DD-<branch>.md#<entry_id>]
```

Правила:
- Новые записи добавляй ТОЛЬКО в КОНЕЦ файла
- Если файл отсутствует — создай с заголовком
- Обновляй ВСЕ релевантные теги, не только один

---

## ✅ Чеклист самоконтроля

Перед началом:
- [ ] Пройден Уровень 1 (быстрая оценка)
- [ ] Если Уровень 1 = ✅: выполнен Уровень 2 (прочитаны теги в параллель)
- [ ] Учтён найденный контекст

После завершения:
- [ ] Определены ВСЕ релевантные скоупы
- [ ] Запись добавлена в diary (в конец)
- [ ] Обновлены ВСЕ tags/ (в конец)
- [ ] Отправлено: «✅ Документация обновлена: …»

---

Примечание: В рамках этого правила запрещено публиковать секреты/токены. Соблюдай краткость (≤ 30 строк на запись), следуй архитектурным паттернам проекта и Laravel 10. Для фронтенда напоминай про `npm run build`/`npm run dev`, если изменения не видны.

